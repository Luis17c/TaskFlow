import { User } from "@/infra/mongoose/schemas/User"

export const userResolvers = {
    Query: {
        async getUsers () {
            
        }
    },

    Mutation: {
        async createUser () {

        }
    }
}